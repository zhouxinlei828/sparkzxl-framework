<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false" scan="true" scanPeriod="1 minutes">
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- 应用名称 -->
    <springProperty name="applicationName" scope="context" source="spring.application.name" defaultValue="application"/>
    <!-- 应用名称 -->
    <springProperty name="environment" scope="context" source="spring.profiles.active" defaultValue="dev"/>
    <!-- 日志文件保存路径 -->
    <springProperty name="logHome" scope="context" source="logging.file.path" defaultValue="${user.home}/logs"/>

    <!-- 日志显隐控制方式 -->
    <springProperty name="enableConsole" scope="context" source="logging.enable-console" defaultValue="true"/>
    <springProperty name="enableJson" scope="context" source="logging.file.enable-json" defaultValue="false"/>
    <springProperty name="enableAlarm" scope="context" source="logging.alarm.enabled" defaultValue="false"/>
    <springProperty name="robotId" scope="context" source="logging.alarm.robotId" defaultValue="false"/>

    <springProperty name="enableFile" scope="context" source="logging.file.enable" defaultValue="false"/>
    <springProperty name="maxHistory" scope="context" source="logging.file.maxHistory" defaultValue="7"/>
    <springProperty name="maxFileSize" scope="context" source="logging.file.maxFileSize" defaultValue="10MB"/>
    <springProperty name="totalSizeCap" scope="context" source="logging.file.totalSizeCap" defaultValue="10GB"/>

    <!-- kafka 配置 -->
    <springProperty name="enableKafka" scope="context" source="logging.kafka.enable" defaultValue="false"/>
    <springProperty name="kafka.servers" scope="context" source="logging.kafka.servers"/>
    <springProperty name="kafka.topic" scope="context" source="logging.kafka.topic"/>
    <springProperty name="kafka.linger" scope="context" source="logging.kafka.linger" defaultValue="1000"/>
    <springProperty name="kafka.acks" scope="context" source="logging.kafka.acks" defaultValue="0"/>
    <springProperty name="kafka.maxBlock" scope="context" source="logging.kafka.maxBlock" defaultValue="0"/>

    <springProperty scope="context" name="log.level.console" source="logging.level.console" defaultValue="INFO"/>

    <!--阿里云日志服务配置 正式环境-->
    <springProperty name="enableAliYunLog" scope="context" source="logging.aliYunLog.enabled"
                    defaultValue="false"/>
    <springProperty name="endpoint" scope="context" source="logging.aliYunLog.endpoint"
                    defaultValue=""/>
    <springProperty name="accessKeyId" scope="context" source="logging.aliYunLog.accessKeyId" defaultValue=""/>
    <springProperty name="accessKeySecret" scope="context" source="logging.aliYunLog.accessKeySecret"
                    defaultValue=""/>
    <springProperty name="project" scope="context" source="logging.aliYunLog.project"
                    defaultValue="gongbaojin"/>
    <springProperty name="logStore" scope="context" source="logging.aliYunLog.logStore" defaultValue=""/>
    <springProperty name="totalSizeInBytes" scope="context" source="aliYunLog.config.totalSizeInBytes"
                    defaultValue="104857600"/>
    <springProperty name="maxBlockMs" scope="context" source="logging.aliYunLog.maxBlockMs" defaultValue="0"/>
    <springProperty name="ioThreadCount" scope="context" source="logging.aliYunLog.ioThreadCount"
                    defaultValue="8"/>
    <springProperty name="batchSizeThresholdInBytes" scope="context"
                    source="logging.aliYunLog.batchSizeThresholdInBytes" defaultValue="524288"/>
    <springProperty name="batchCountThreshold" scope="context" source="logging.aliYunLog.batchCountThreshold"
                    defaultValue="4096"/>
    <springProperty name="lingerMs" scope="context" source="logging.aliYunLog.lingerMs" defaultValue="2000"/>
    <springProperty name="retries" scope="context" source="logging.aliYunLog.retries" defaultValue="10"/>
    <springProperty name="baseRetryBackoffMs" scope="context" source="logging.aliYunLog.baseRetryBackoffMs"
                    defaultValue="100"/>
    <springProperty name="maxRetryBackoffMs" scope="context" source="logging.aliYunLog.maxRetryBackoffMs"
                    defaultValue="50000"/>
    <springProperty name="timeFormat" scope="context" source="logging.aliYunLog.timeFormat"
                    defaultValue="yyyy-MM-dd HH:mm:ss.SSS"/>
    <springProperty name="timeZone" scope="context" source="logging.aliYunLog.timeZone"
                    defaultValue="Asia/Shanghai"/>

    <!-- 控制台 日志格式化 -->
    <property name="CONSOLE_LOG_PATTERN"
            value="%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}){faint} %blue(tenantId:%X{tenantId:-N/A}) %boldMagenta(traceId:%X{traceId:-N/A}) %highlight(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} [%X{tid}] %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{15}){boldCyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <property name="FILE_LOG_PATTERN"
            value="%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} :%X{tenantId:-N/A} traceId:%X{traceId:-N/A} ${LOG_LEVEL_PATTERN:-%5p} ${PID:- } [%X{tid}] [%-40.40logger{15}] : %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <!-- 控制台 Appender -->
    <appender name="ConsoleOutput" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <!-- 日志的格式化 -->
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.mdc.TraceIdMDCPatternLogbackLayout">
                <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            </layout>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!--每天记录日志到文件appender-->
    <appender name="FileOutput" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logHome}/${applicationName}.log</file>
        <immediateFlush>false</immediateFlush>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${logHome}/${applicationName}-%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxHistory>${maxHistory}</maxHistory>
            <maxFileSize>${maxFileSize}</maxFileSize>
            <totalSizeCap>${totalSizeCap}</totalSizeCap>
        </rollingPolicy>
        <if condition='property("enableJson").contains("true")'>
            <then>
                <encoder charset="UTF-8" class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                    <providers class="net.logstash.logback.composite.loggingevent.LoggingEventJsonProviders">
                        <pattern>
                            <pattern>
                                {"date": "%d{yyyy-MM-dd HH:mm:ss.SSS}",
                                "level": "%level",
                                "application": "${applicationName}",
                                "environment": "${environment}",
                                "tenantId": "%X{tenantId}",
                                "traceId": "%X{traceId}",
                                "tid": "%X{tid}",
                                "pid": "${PID:-}",
                                "thread": "%thread",
                                "class": "%logger{15}",
                                "message": "%message"}
                            </pattern>
                        </pattern>
                    </providers>
                </encoder>
            </then>
            <else>
                <!-- 日志的格式化 -->
                <encoder>
                    <pattern>${FILE_LOG_PATTERN}</pattern>
                    <charset>utf8</charset>
                </encoder>
            </else>
        </if>
    </appender>

    <!-- 日志信息发送kafka -->
    <appender name="kafkaAppender" class="com.github.danielwegener.logback.kafka.KafkaAppender">
        <encoder>
            <pattern>
                {"date": "%d{yyyy-MM-dd HH:mm:ss.SSS}",
                "level": "%level",
                "application": "${applicationName}",
                "environment": "${environment}",
                "tenantId": "%X{tenantId}",
                "tid": "%X{tid}",
                "pid": "${PID:-}",
                "thread": "%thread",
                "class": "%logger{40}",
                "message": "%message"}
            </pattern>
            <charset>utf8</charset>
        </encoder>
        <!-- kafka topic -->
        <topic>${kafka.topic}</topic>
        <keyingStrategy class="com.github.danielwegener.logback.kafka.keying.NoKeyKeyingStrategy"/>
        <deliveryStrategy class="com.github.danielwegener.logback.kafka.delivery.AsynchronousDeliveryStrategy"/>
        <!-- kafka 地址 -->
        <producerConfig>bootstrap.servers=${kafka.servers}</producerConfig>
        <!-- ack确认机制  -->
        <producerConfig>acks=${kafka.acks}</producerConfig>
        <!-- 等待最多1000 毫秒并收集日志消息，然后将它们作为批处理发送 -->
        <producerConfig>linger.ms=${kafka.linger}</producerConfig>
        <!-- 即使生产者缓冲区已满，也不要阻塞应用程序而是开始丢弃消息 -->
        <producerConfig>max.block.ms=${kafka.max.block}</producerConfig>
        <!-- 定义一个客户端 ID，用于在 kafka broker 中识别自己 -->
        <producerConfig>client.id=${HOSTNAME}-${CONTEXT_NAME}-logback-relaxed</producerConfig>
    </appender>

    <!-- skywalking grpc日志传输-->
    <appender name="grpc-log" class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.log.GRPCLogClientAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.mdc.TraceIdMDCPatternLogbackLayout">
                <pattern>%m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}</pattern>
            </layout>
        </encoder>
    </appender>

    <if condition='property("enableAliYunLog").contains("true")'>
        <then>
            <appender name="aliyun-sls-log" class="com.aliyun.openservices.log.logback.LoghubAppender">
                <!--必选项-->
                <!-- 账号及网络配置 -->
                <endpoint>${endpoint}</endpoint>
                <accessKeyId>${accessKeyId}</accessKeyId>
                <accessKeySecret>${accessKeySecret}</accessKeySecret>

                <!-- sls 项目配置 -->
                <project>${project}</project>
                <logStore>${logStore}</logStore>
                <!--必选项 (end)-->

                <!-- 可选项 -->
                <!--<topic>lws-log</topic>-->

                <!-- 可选项 详见 '参数说明'-->
                <!--单个 producer 实例能缓存的日志大小上限，默认为 100MB。-->
                <totalSizeInBytes>${totalSizeInBytes}</totalSizeInBytes>
                <!--如果 producer 可用空间不足，调用者在 send 方法上的最大阻塞时间，默认为 60 秒。为了不阻塞打印日志的线程
                ，强烈建议将该值设置成 0。-->
                <maxBlockMs>${maxBlockMs}</maxBlockMs>
                <!--执行日志发送任务的线程池大小，默认为可用处理器个数。-->
                <ioThreadCount>${ioThreadCount}</ioThreadCount>
                <!--当一个 ProducerBatch 中缓存的日志大小大于等于 batchSizeThresholdInBytes 时，
                该 batch 将被发送，默认为 512 KB，最大可设置成 5MB。-->
                <batchSizeThresholdInBytes>${batchSizeThresholdInBytes}</batchSizeThresholdInBytes>
                <!--当一个 ProducerBatch 中缓存的日志条数大于等于 batchCountThreshold 时，
                该 batch 将被发送，默认为 4096，最大可设置成 40960。-->
                <batchCountThreshold>${batchCountThreshold}</batchCountThreshold>
                <!-- 一个 ProducerBatch 从创建到可发送的逗留时间，默认为 2 秒，最小可设置成 100 毫秒。-->
                <lingerMs>${lingerMs}</lingerMs>
                <!--  如果某个 ProducerBatch 首次发送失败，能够对其重试的次数，默认为 10 次。
                #如果 retries 小于等于 0，该 ProducerBatch 首次发送失败后将直接进入失败队列。-->
                <retries>${retries}</retries>
                <!-- 首次重试的退避时间，默认为 100 毫秒。-->
                <baseRetryBackoffMs>${baseRetryBackoffMs}</baseRetryBackoffMs>
                <!--重试的最大退避时间，默认为 50 秒。-->
                <maxRetryBackoffMs>${maxRetryBackoffMs}</maxRetryBackoffMs>


                <!-- 可选项 通过配置 encoder 的 pattern 自定义 log 的格式 -->
                <encoder>
                    <pattern>%d %-5level [%thread] %logger{0}: %msg</pattern>
                </encoder>

                <!-- 可选项 设置 time 字段呈现的格式 -->
                <timeFormat>${timeFormat}</timeFormat>
                <!-- 可选项 设置 time 字段呈现的时区 -->
                <timeZone>${timeZone}</timeZone>
                <mdcFields>THREAD_ID,MDC_KEY</mdcFields>
            </appender>
            <appender name="AsyncAaLiYunLog" class="ch.qos.logback.classic.AsyncAppender">
                <discardingThreshold>0</discardingThreshold>
                <queueSize>100</queueSize>
                <neverBlock>true</neverBlock>
                <appender-ref ref="aliyun-sls-log"/>
            </appender>
        </then>
    </if>

    <!-- 异步控制台打印 -->
    <appender name="AsyncConsole" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>100</queueSize>
        <neverBlock>true</neverBlock>
        <appender-ref ref="ConsoleOutput"/>
    </appender>

    <!-- 异步输出文件 -->
    <appender name="AsyncFile" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>100</queueSize>
        <neverBlock>true</neverBlock>
        <appender-ref ref="FileOutput"/>
    </appender>

    <!-- 异步传递策略，建议选择异步，不然连接kafka失败，会阻挡服务启动 -->
    <appender name="AsyncKafka" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>4096</queueSize>
        <neverBlock>true</neverBlock>
        <includeCallerData>true</includeCallerData>
        <appender-ref ref="kafkaAppender"/>
    </appender>


    <appender name="AlarmLog" class="com.github.sparkzxl.log.AlarmLogLogbackAsyncAppender">
        <robotId>${robotId}</robotId>
        <appender-ref ref="ConsoleOutput"/>
    </appender>

    <logger name="springfox.documentation" level="ERROR"/>
    <logger name="com.alibaba.nacos" level="ERROR"/>

    <root level="${log.level.console}">
        <appender-ref ref="grpc-log"/>
        <if condition='property("enableFile").contains("true")'>
            <then>
                <appender-ref ref="AsyncFile"/>
            </then>
        </if>
        <if condition='property("enableConsole").contains("true")'>
            <then>
                <appender-ref ref="AsyncConsole"/>
            </then>
        </if>
        <if condition='property("enableKafka").contains("true")'>
            <then>
                <appender-ref ref="AsyncKafka"/>
            </then>
        </if>
        <if condition='property("enableAlarm").contains("true")'>
            <then>
                <appender-ref ref="AlarmLog"/>
            </then>
        </if>
        <if condition='property("enableAliYunLog").contains("true")'>
            <then>
                <appender-ref ref="AsyncAaLiYunLog"/>
            </then>
        </if>
    </root>
</configuration>
